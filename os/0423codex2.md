# HALF main 分支开源前最终审查

审查时间：2026-04-23

审查对象：`main` 分支工作树

结论：未发现已提交的真实密钥、证书、数据库、`.env`、构建输出或明显的内网地址；当前仍有 1 处公开信息需要在正式开源前确认是否保留。

## Findings

### 1. `SECURITY.md` 公开了项目联系邮箱

- 位置：`SECURITY.md:5`
- 内容：安全漏洞上报邮箱现写为 `osscontact@163.com`
- 结论：该邮箱已切换为项目专用公开联系邮箱，不再构成个人主邮箱暴露问题。
- 建议：可保留；如后续需要区分安全漏洞与一般联系，再拆分专用地址。

### 2. `CITATION.cff` 指向 `keting/half`

- 位置：`CITATION.cff:12`
- 内容：`repository-code: "https://github.com/keting/half"`
- 结论：根据当前确认，待开源仓库就是 `keting/half`，因此这项不是问题。
- 备注：本次记录保留此项，仅用于说明已核对过仓库归属，避免后续重复排查。

## 已检查且未发现问题

- 未发现已提交的真实 secret/token/password/private key
- 未发现 `.env` 实际文件，仅有 `src/.env.example`
- 未发现 `.db` / `.sqlite` / `outputs/` / `data/` 等运行数据被跟踪
- 未发现绝对本地路径如 `/home/...`、`/Users/...`
- 未发现明显的私有仓库 URL、内网域名、内网 IP、VPN/飞书/企业微信/Jira/Confluence 等内部协作痕迹

## 备注

- `README.md:72` 中的 `HALF_ADMIN_PASSWORD='LocalDevA1'` 是示例开发密码，不是泄露项；但它是一个固定示例值。若你希望文档更保守，可以改回占位符，避免用户直接照抄到长期环境。
- 当前本地 git remote 仍是 `git@github.com:keting/half.git`，这不是仓库内容，不会随代码一起发布；这里只作为发布前人工确认项。
